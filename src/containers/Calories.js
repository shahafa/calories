import React from 'react';
import PropTypes from 'prop-types';

const Calories = props => {
  return (
    <div>
      Hello
    </div>
  );
};

Calories.propTypes = {
  
};

export default Calories;
